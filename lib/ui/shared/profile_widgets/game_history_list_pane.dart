import 'package:flutter/material.dart';

class GameHistoryListPane extends StatelessWidget {
  const GameHistoryListPane({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.green);
  }
}
